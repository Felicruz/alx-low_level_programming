#include "main.h"

/**
 *_islower - checks if a letter is a lowercase.
 *@c: the main argument
 *Return: returns zero or one according to the condition.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
